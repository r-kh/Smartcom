# 📦 Игнорируем байт-код Python (иначе закоммитишь мусор скомпилированных файлов)
__pycache__/        # Папка с байткодом Python (иначе будет мусор в репозитории)
*.py[cod]           # Файлы байткода .pyc, .pyo, .pyd (иначе коммитятся временные файлы)

# 🧪 Игнорируем тестовые артефакты (иначе тестовые данные попадут в репозиторий)
.coverage           # Отчёты покрытия тестами (иначе коммит тестовых данных)
.pytest_cache/      # Кэш pytest (иначе будут ненужные файлы)
.cache/             # Общий кэш (иначе будут лишние данные)

# 🔐 Игнорируем переменные окружения (иначе можно случайно слить секреты)
.env                # Файлы с переменными окружения (иначе коммит секретов)
*.env               # Все файлы с расширением .env (иначе риск слить ключи)

# 🧪 Игнорируем виртуальные окружения (иначе закоммитишь библиотеки и мусор)
venv/               # Виртуальное окружение (иначе лишние зависимости)
.venv/              # Альтернативное имя виртуального окружения (иначе мусор)
env/                # Виртуальное окружение (иначе большая папка в репо)
ENV/                # Виртуальное окружение (иначе захламление)

# 🧠 Игнорируем IDE и редакторские настройки (иначе конфликты настроек)
.idea/              # Папка настроек PyCharm/IntelliJ (иначе конфликты)
*.iml               # Файлы модулей JetBrains (иначе не нужны)
.vscode/            # IDE файлы в Windows

# 💾 Игнорируем локальные базы данных SQLite (иначе скоммитишь базу с данными)
*.sqlite3           # Файлы SQLite БД (иначе зальёшь базу)
*.db                # Другие БД-файлы (иначе захламление)

# 📝 Игнорируем временные логи и PID-файлы (иначе коммит логов и процессов)
*.log               # Лог-файлы (иначе засорение репозитория)
*.pid               # PID-файлы процессов (иначе мусор)

# 🍎 Игнорируем macOS системные файлы (иначе случайно попадут в репозиторий)
.DS_Store           # Системные файлы Mac (иначе мусор)

# 🪟 Игнорируем Windows служебные файлы (иначе ненужные файлы)
Thumbs.db           # Кеш миниатюр Windows (иначе захламит репозиторий)

# 💡 Игнорируем временные файлы редакторов (иначе коммит временных файлов)
*.swp               # Файлы Vim автосохранения (иначе мусор)
*.swo               # Аналогично (иначе мусор)
*~                  # Резервные копии файлов (иначе хлам)

# 🌐 Игнорируем временные и служебные папки (иначе попадут временные данные)
uploads/            # Папка с загруженными файлами (иначе можно закоммитить чужие файлы)
tmp/                # Временные файлы и кэш (иначе мусор)